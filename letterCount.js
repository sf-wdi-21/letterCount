function letterCount(word) {

  // code in here

}


/*
 Best if you don't code out here.
 Write your "driver code" in `index.js` instead
 and then run `node index.js` to try it out.
*/



// export the letterCount so we can use it elsewhere (ignore)
module.exports = letterCount;
